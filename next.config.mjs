/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/filipebcs8',
  assetPrefix: '/filipebcs8/',
  trailingSlash: true,
  images: { unoptimized: true },
}

export default nextConfig
